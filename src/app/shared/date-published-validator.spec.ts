import { DatePublishedValidator } from './date-published-validator';

describe('DatePublishedValidator', () => {
  it('should create an instance', () => {
    expect(new DatePublishedValidator()).toBeTruthy();
  });
});
